# Mode démonstration : `npm run dev:demo`. Aucun secret ici — ce fichier se
# commite. Vite le charge PAR-DESSUS `.env` en `--mode demo` : le jeu fictif
# remplace la base et l'authentification, bandeau permanent à l'écran.
VITE_DEMO=1
