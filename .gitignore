node_modules
dist
.env
.env.local
.vercel
*.local
.DS_Store

# Cache incrémental de TypeScript : régénéré à chaque build, propre à la machine.
*.tsbuildinfo

.vercel

# Cache du CLI Supabase (projet lié, binaires) : propre à la machine.
supabase/.temp
